# Domain, die per DNS auf diesen VPS zeigt (A-Record). Caddy holt dafür automatisch TLS.
DOMAIN=backup.deine-domain.de

# Erlaubter Browser-Origin (die URL, unter der die App läuft). Genau setzen (kein "*").
ALLOW_ORIGIN=https://lernapp-mauve.vercel.app
